tasks:
  assembleWeb:
    action: !com.emilflach.lokcal.plugins.webdist.assembleWeb
      moduleRootDir: ${module.rootDir}
  runWeb:
    action: !com.emilflach.lokcal.plugins.webdist.runWeb
      moduleRootDir: ${module.rootDir}
      settings: ${pluginSettings}

commands:
  - assembleWeb
  - runWeb
