tasks:
  generateSecrets:
    action: !com.emilflach.lokcal.plugins.secrets.generateSecrets
      moduleRootDir: ${module.rootDir}
      outputDir: ${taskOutputDir}
      settings: ${pluginSettings}

generated:
  sources:
    - directory: ${tasks.generateSecrets.action.outputDir}
