product: jvm/amper-plugin

description: Generates a Kotlin object of build-time secrets (Kroger API keys) from local.properties / env vars. Replaces the Gradle buildSrc GenerateSecretsTask.

pluginInfo:
  id: secrets
  settingsClass: com.emilflach.lokcal.plugins.secrets.Settings

settings:
  jvm:
    jdk:
      version: 21
